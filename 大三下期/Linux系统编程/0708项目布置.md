# 0708

```c
cloud.tencent
//公网ip
81.68.250.194

sudo apt update
sudo apt upgrade
sudo apt install build-essential(有cc，gcc，make等)
sudo apt install fish
sudo apt install vim
sudo apt install nginx（网络相关）
/var/www/html

查看tcp的端口
sudo netstat -tpnl(tcp,port等)
查看udp的端口
sudo netstat -upnl(udp,port等)
ps -ef（查看进程相关）

./hw1 & （后台运行）

与终端分离
nohup ./hw1 &

变成守护进程
```

## 任务

1. 56-61章看看
   - 60章讲迭代、并发服务器设计
2. 看看多线程实现，再看看epoll实现网络编程
3. 完成项目

### 项目

- HTTP服务器
