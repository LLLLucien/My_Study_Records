# openSSL

## MD5、SHA1、SHA256
- 数字指纹、摘要，唯一性
- 加密
- 不可逆
- 密码 SHA256 + 盐

## Base64 编码与解码
- 二进制转为文本格式
- 文件增大 1/3
- 双向
- 加密
- 64种字符 a-z 26*2, 0-9, +=

## 加密与解密
- RSA 非对称密钥加密与解密
- 

## 任务

### 1. SHA256 + 盐 密码加密策略

#### 原理
- **SHA256**: 一种密码哈希函数，将任意长度的数据转换为固定长度（256位/64个十六进制字符）的哈希值
- **盐（Salt）**: 随机生成的字符串，与密码混合后再进行哈希
- **核心目的**: 防止彩虹表攻击，即使相同密码也会产生不同哈希值

#### 实现步骤
1. **生成盐**: 使用加密安全的随机数生成器（如 OpenSSL 的 `RAND_bytes`）生成 16-32 字节的随机盐
2. **混合密码与盐**: 将密码和盐拼接（通常格式：`$salt$password` 或 `password$salt`）
3. **迭代哈希**: 对混合后的数据进行多次哈希迭代（推荐至少10000次）
4. **存储结果**: 将盐和最终哈希值一起存储（格式如：`$salt$hash`）

#### 验证流程
1. 从数据库获取存储的盐和哈希值
2. 使用相同的盐与用户输入的密码混合
3. 进行相同次数的哈希迭代
4. 比较计算结果与存储的哈希值是否一致

#### 关键代码示例
```c
#include <openssl/sha.h>
#include <openssl/rand.h>

// 生成随机盐
void generate_salt(unsigned char *salt, int len) {
    RAND_bytes(salt, len);
}

// SHA256哈希函数
void sha256_hash(unsigned char *input, size_t input_len, unsigned char *output) {
    SHA256_CTX ctx;
    SHA256_Init(&ctx);
    SHA256_Update(&ctx, input, input_len);
    SHA256_Final(output, &ctx);
}

// 带盐的密码哈希
void hash_password(const char *password, unsigned char *salt, 
                   unsigned char *hash, int iterations) {
    unsigned char temp[SHA256_DIGEST_LENGTH];
    unsigned char combined[1024];
    
    // 混合密码和盐
    snprintf((char*)combined, sizeof(combined), "%s%s", password, (char*)salt);
    
    // 初始哈希
    sha256_hash(combined, strlen((char*)combined), temp);
    
    // 迭代哈希
    for (int i = 1; i < iterations; i++) {
        sha256_hash(temp, SHA256_DIGEST_LENGTH, temp);
    }
    
    memcpy(hash, temp, SHA256_DIGEST_LENGTH);
}
```

---

### 2. Base64 的使用场景

#### 核心用途
将二进制数据转换为 ASCII 文本格式，便于在文本协议中传输

#### 主要应用场景

| 场景 | 说明 |
|------|------|
| **数据传输** | 在 JSON、XML、URL 等文本协议中传输二进制数据 |
| **数据存储** | 在数据库或配置文件中存储二进制数据 |
| **图片嵌入** | 在 HTML/CSS 中内嵌图片（Data URI scheme） |
| **邮件附件** | 在 SMTP 协议中传输附件 |
| **API 接口** | RESTful API 中传输文件或二进制数据 |
| **证书编码** | 编码 SSL/TLS 证书和密钥 |

#### 典型应用示例

**1. HTML 内嵌图片**
```html
<img src="data:image/png;base64,iVBORw0KGgoAAAANSUhEUg..." />
```

**2. JSON 传输二进制数据**
```json
{
    "file_name": "document.pdf",
    "content": "JVBERi0xLjMKJcfsj6IKNSAwIG9iago8PC9MZW5ndG..."
}
```

**3. 邮件附件编码**
```
Content-Type: application/pdf; name="report.pdf"
Content-Transfer-Encoding: base64
Content-Disposition: attachment; filename="report.pdf"

JVBERi0xLjMKJcfsj6IKNSAwIG9iago8PC9MZW5ndG...
```

#### 优缺点

| 优点 | 缺点 |
|------|------|
| 跨平台兼容 | 数据体积增大约 33% |
| 纯文本格式，易于处理 | 不提供加密保护 |
| 广泛支持 | 编码解码需要额外计算 |


## 示例
```c
// base64.c
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

#include <openssl/evp.h>
#include <openssl/bio.h>        // basic input output, 类似 FILE
#include <openssl/buffer.h>

// 动态内存分配
char *encode(char *str)
{
    // 过滤器
    BIO *b64 = BIO_new(BIO_f_base64());
    // source 为内存的 bio
    BIO *bio = BIO_new(BIO_s_mem());
    // 给bio加上 base64 过滤器
    bio = BIO_push(b64, bio);
    // 不换行
    BIO_set_flags(bio, BIO_FLAGS_BASE64_NO_NL);

    // 写操作
    BIO_write(bio, str, strlen(str));
    // 刷新缓冲区
    BIO_flush(bio);
    // 内存缓冲区
    BUF_MEM *buf;
    BIO_get_mem_ptr(bio, &buf);

    // 动态分配堆空间
    char *data = malloc(buf->length + 1);
    memcpy(data, buf->data, buf->length);
    data[buf->length] = 0;
    BIO_free_all(bio);

    return data;
}

char *decode(char *data, int *len)
{
    // filter 过滤器，装饰器模式
    BIO *b64 = BIO_new(BIO_f_base64());

    int input_len = strlen(data);
    printf("input: %d\n", input_len);

    // base64 要解码的数据写入 BIO 的内存缓冲区
    BIO *bio = BIO_new_mem_buf(data, input_len);
    bio = BIO_push(b64, bio);
    BIO_set_flags(bio, BIO_FLAGS_BASE64_NO_NL);
    
    char *buf = malloc(input_len);
    *len = BIO_read(bio, buf, input_len);
    printf("ouput: %d\n", *len);
    BIO_free_all(bio);

    return buf;
}

void encode_file(char *in_file, char *out_file)
{
    // 读
    BIO *in = BIO_new_file(in_file, "rb");
    // 写
    BIO *out = BIO_new_file(out_file, "w");

    // 给输出流加了过滤器
    BIO *b64 = BIO_new(BIO_f_base64());
    out = BIO_push(b64, out);


    char buf[1024];
    int n;
    while ((n = BIO_read(in, buf, sizeof(buf))) > 0)
    {
        BIO_write(out, buf, n);
    }
    BIO_flush(out);
    BIO_free_all(in);
    BIO_free_all(out);
    printf("ok\n");
}

void decode_file(char *in_file, char *out_file)
{
    // FILE *fp = fopen(in_file, "w");
    // fread();
    // fwrite();
    // fflush();
    // fclose();

    BIO *in = BIO_new_file(in_file, "r");
    BIO *out = BIO_new_file(out_file, "wb");

    BIO *b64 = BIO_new(BIO_f_base64());
    in = BIO_push(b64, in);

    char buf[1024];
    int n;
    while ((n = BIO_read(in, buf, sizeof(buf))) > 0)
    {
        BIO_write(out, buf, n);
    }
    BIO_flush(out);
    BIO_free_all(in);
    BIO_free_all(out);
    printf("OK\n");
}

int main(int argc, char const *argv[])
{
    // 文本编码与解码
    // hello
    // aGVsbG8=
    // char *str = "hello";
    // char *base64 = encode(str);
    // printf("%s\n", base64);

    // char *data = "aGVsbG8=";

    // int len = 0;
    // char *text = decode(data, &len);
    // printf("%s\n", text);
    // printf("%.*s\n", len, text);
    // free(text);

    // 文件编码与解码
    // data:pdf;base64,xxxxxxxxxxxxxxx
    // encode_file("tux.png", "tux.txt");

    // png, jpg, jpeg, gif, webp ...
    // 
    decode_file("tux.txt", "t.png");

    return 0;
}


```

